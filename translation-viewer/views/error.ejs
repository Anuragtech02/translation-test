<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { padding: 2em; }
        .error-container { max-width: 600px; margin: auto; padding: 2em; border: 1px solid #dc3545; border-radius: 5px; background-color: #f8d7da; }
        pre { background-color: #f1f1f1; padding: 1em; border-radius: 4px; white-space: pre-wrap; word-wrap: break-word; }
    </style>
</head>
<body>
    <div class="error-container text-center">
        <h1 class="text-danger"><%= message %></h1>
        <h2 class="text-muted"><%= error.status || 'Error' %></h2>
        <% if (error.stack && process.env.NODE_ENV !== 'production') { %>
             <!-- Only show stack in non-production environments -->
             <pre class="text-start mt-4"><code><%= error.stack %></code></pre>
        <% } %>
        <a href="/" class="btn btn-primary mt-3">Go to Homepage</a>
    </div>
</body>
</html>
